name: Pipeline

on:
  push:
    branches:
      - main
      - feature/pipline
  workflow_dispatch:

jobs:
    call_trivy:
        uses: ./.github/workflows/trivy.yml
        secrets: inherit
    call_docker_scout:
      uses: ./.github/workflows/docker_scout.yml
      secrets: inherit
